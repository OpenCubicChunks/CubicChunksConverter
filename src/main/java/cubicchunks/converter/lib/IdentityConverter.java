package cubicchunks.converter.lib;

import java.nio.file.Path;

public class IdentityConverter implements ISaveConverter {
	@Override public void convert(IProgress progress, Path srcDir, Path dstDir) {

	}
}
